package com.projet.internmatch.Repository;

import com.projet.internmatch.entity.CV;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CVRepository extends JpaRepository<CV,Long> {
}
